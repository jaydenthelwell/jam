[build]
  command = "export PG_CONFIG=$(which pg_config) && bundle install && bundle exec rake assets:precompile"
  publish = "public"

[[plugins]]
  package = "netlify-plugin-apt"

  [plugins.inputs]
  packages = [
    "libpq-dev"
  ]
