[build]
  command = "bundle install --deployment && bundle exec rake assets:precompile"
  publish = "public"

[[plugins]]
  package = "netlify-plugin-apt"

  [plugins.inputs]
  packages = [
    "libpq-dev"
  ]
